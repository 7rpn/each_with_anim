require 'spec_helper'

describe EachWithAnim do
  it 'has a version number' do
    expect(EachWithAnim::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
